#include<bits/stdc++.h>
using namespace std;
const int mod=602214179,mxm=6;
long long a[1<<6][1<<6];
long long n,m,t,dp[mxm];
void calc()
{
    for(int i=0;i<(1<<m);i++)
    {
        for(int j=0;j<(1<<m);j++)
        {
            memset(dp,0,sizeof(dp));
            if(j&(1<<k))
        }
    }
}
