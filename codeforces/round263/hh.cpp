#include<bits/stdc++.h>
using namespace std;
const int nmax=3*1e5+9;
long long psum[nmax];
int a[nmax];
int main(){
   ios_base::sync_with_stdio(false);
   cin.tie(0);
   cout<<100000<<" "<<100000<<"\n";
   cout<<"B";
   for(int i=1;i<=99999;i++)cout<<"A";
   cout<<"\n";
}
