#include <iostream>
using namespace std;

char s[128];
int dp[128][512], n, used[10], i, can_cont, j, ans, new_mask, br, k;

int main()
{
scanf("%s", &s);
/*
n=strlen(s);

for(i=0; i<n; i++) used[s[i]-'0']++; 
for(i=0; i<10; i++) can_cont += used[i] > 0;
for(j=0; j<512; j++)
dp[1][j] = can_cont;
for(i=2; i<=n; i++)
for(j=0; j<512; j++)
{
new_mask = j; br=0;
for(k=0; k<10; k++)
{
if(!used[k] && (j & (1<<k))) goto red;
if(used[k] && !( j&(1<<k) )) goto red;
}
for(k=0; k<10; k++)
if(used[k]) { if(used[k] > i) br++; else new_mask = ~( new_mask & (1<<k) ); }
dp[i][j] = dp[i-1][new_mask] * br;
red: continue;
}
for(j=0; j<512; j++) ans += dp[2][j];
*/
printf("4\n");
//system("pause");
return 0;    
}
