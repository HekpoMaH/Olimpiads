#include <iostream>

using namespace std;

double arr[500];
int line[500];
int main(){
    int n, p;
    cin >> n >> p;
    for (int i=0; i<n; ++i) cin >> arr[i];
    
    --p;
    for (int i=0; i<p; ++i) line[i]=i;
    
    for (int i=p-1; i>=0; --i)   ;
        
    return 0;
}
    
    
    
        
    
    
