#include<iostream>
using namespace std;
int main()
{
    long long int n,m,win,lose;
    cin>>n>>m;
    do
    {
       for(n=1;n<=26;n++)
       if(m==n);
       if(m*n);
       if(m*n<=26);
       cout<<win<<endl;
       if(m==n);

       cout<<lose<<endl;
    }
    while(win,lose);
return 0;
}